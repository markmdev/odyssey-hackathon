# Odyssey SDK (client-side — exposed to browser)
NEXT_PUBLIC_ODYSSEY_API_KEY=ody_...

# OpenAI (server-side only — used in API routes)
OPENAI_API_KEY=sk-...

# Gemini (server-side only — used in API routes)
GEMINI_API_KEY=...
