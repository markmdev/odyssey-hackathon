# 2026-02-05 — Odyssey Hackathon

Back to [MEMORY.md](../MEMORY.md)

## Context
- Mark is at an Odyssey hackathon, building something with their world model SDK
- Repo: https://github.com/markmdev/odyssey-hackathon
- API keys stored in `.env` (gitignored)

## Key Files
- `playground.html` + `src/playground.ts` — SDK test playground (preserved)
- `index.html` + `src/main.ts` — currently the game engine (will become Memory Palace)
- `.env` — API keys: `VITE_ODYSSEY_API_KEY`, `VITE_OPENAI_API_KEY`, need `VITE_GEMINI_API_KEY`
- `.meridian/api-docs/odyssey-sdk.md` — Odyssey SDK reference
- `.meridian/api-docs/gemini-image-gen.md` — Gemini image generation reference
- `.meridian/interaction-tips.md` — Odyssey prompt engineering guide
- `.meridian/odyssey-2-pro-overview.md` — what the world model is

## Journey: Game Engine → Memory Palace

### Game engine attempt (abandoned)
- WASD keyboard → `interact()` calls mapped to scenario prompts
- 3 scenarios: mountain climb, dungeon crawl, space walk
- **Didn't work well** — Odyssey is too imprecise for game-like controls
- From Odyssey team: model is bad at subtle movements, needs very detailed full-scene prompts
- `interact()` uses WebRTC data channel (not HTTP, won't show in Network tab)

### Memory Palace (current direction)
Mark's idea: AI-generated visual memory palace using Method of Loci. Pipeline:
1. **GPT-5.2** generates palace plan (rooms, mnemonics)
2. **Gemini 3 Pro** (`gemini-3-pro-image-preview`) generates vivid room images (base64 PNG)
3. **Odyssey I2V** animates images into living video via `startStream({ image, prompt })`
4. **Frontend UI harness** — reliable HTML for progress, navigation, text (not dependent on Odyssey)

Key decisions:
- Deterministic pre-built challenges, not freeform input
- Odyssey is the visual wow layer, not the core product
- Each room = full detailed scene (plays to model strengths)

## Gemini API ("Nano Banana Pro")
- SDK: `@google/genai` (NOT the deprecated `@google/generative-ai`)
- Model IDs: `gemini-2.5-flash-image` (fast/cheap) or `gemini-3-pro-image-preview` (pro, text rendering, 4K)
- Returns base64 PNG inline (not URLs) — convert to Blob for Odyssey I2V
- Works in browser (hackathon OK, production needs proxy)
- Pro model has "thinking" steps (adds latency, better quality)
- `imageSize` must use uppercase K: `"2K"` not `"2k"`

## Issues Encountered
1. Orphaned session blocking reconnect (max 1 concurrent) → `beforeunload` handler + auto-retry
2. WebRTC ICE failure → hackathon WiFi blocking UDP, phone hotspot fixes it
3. Video element growing too tall → `max-height: 70vh`
4. Game interactions underwhelming → model too imprecise for subtle movements

## v2 Improvements (this session)
- Replaced single `odysseyPrompt` per room with `odysseyKeyframes[]` — 3-4 action beats per 10s video
  - t=0 start (with image), t=3s interact, t=6s interact, t=10s end
  - GPT now generates a mini-story sequence per room (e.g. Berlin Wall: stands → cracks → collapses)
- Progressive palace loading — enter on first Gemini image, Odyssey renders in background
  - Static images shown immediately with "Bringing to life..." indicator
  - Videos swap in as they complete
  - Cuts perceived wait from ~4min to ~15-20s
- Per-room individual simulate calls (not batch) — videos trickle in independently
- Resilient Gemini image gen — per-room try/catch, one failure doesn't kill pipeline
- **Prepared scenario mode** for hackathon demo
  - CLI script: `npx tsx scripts/generate-scenario.ts --name X --grade Y`
  - Saves images + videos to `public/scenarios/<name>/`
  - Frontend loads pre-baked scenarios with fake loading simulation (~5s)
  - Real generation hidden behind `?live` URL param
  - `public/scenarios/index.json` lists available scenarios (currently empty)
- Deployment needed — Mark wants to deploy for hackathon presentation

## Scenario Ideas (discussed, not yet generated)
- Solar System (8 planets) — grade 4-6
- Dinosaurs (6 iconic dinos) — grade K-3 or 4-6
- World Wonders — grade 7-9
- Human Body (organs) — grade 4-6
- Ocean Creatures — grade K-3

## Mark's Preferences
- Small, incremental builds — test before going big
- Write everything down in workspace files as memory
- Granular commits
- Novel, non-obvious ideas with real-world utility
- Save insights regularly without being reminded
